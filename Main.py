import time
class learn:
    number_of_participant=input("number of interested individuals:")
    number_of_participant=int(number_of_participant)
    for i in range(1,number_of_participant):
        name=""
        interests=""
        t=0
        choice=""
        def __init__(a):
            a
        def addStacks(self,interest):
            self.interest=interest
            setMentorOrLearner(choice)
        def setAvailableTime():
            time=input("enter a time in the format HH:MM \n")
            h,m=map(int,time.split(":"))
            t=h,m
            return t
            #print time
        def setMentorOrLearner(self,choice):
            self.choice=choice
            if learn.choice=="mentor":
                setAvailableTime()
                print(t)
        def getMentor(stack,time):
            self.stack=stack
            self.time=time
r=learn()
